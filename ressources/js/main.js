class PopCarteTest {
    constructor() {
        this.init();
    }

    /**
     * Initializes the class, gets elements from the DOM, sets event listeners
     */
    init() {
        // TODO: test server connection and display error if not connected

        // Get elements from the DOM

        // Set event listeners
        // City input event listener to get autocomplete suggestions from server

        console.log("PopCarteTest energized !");
    }

    /**
     * Sends parameters to the server to register the card
     */
    validateCard() {
        // Get the data from the form
        // Send the data to the server
        // If the server returns an error, display it
        // If the server returns a success, go to card display
    }

    /**
     * Clears the form and displays the card
     */
    displayCard() {
        // Clear the form
        // Push state to the browser
        // Set logo button eventlistener to go back to the form
        // Get temperature of the city
        // Display the card
    }
}

new PopCarteTest();
